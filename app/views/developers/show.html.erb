<section>
  <div class="container">
    <div class="columns">
      <div class="column is-12">
        <div class="box is-fullwidth datapanel primary">
          <div class="level">
            <div class="level-left">
              <h1 class="title"><b>Overview: <%= @developer.github_user.name ? @developer.github_user.name : @developer.github_user.login %></b></h1>
            </div>
            <div class="level-right">
              <div class="control">
                <%= link_to team_path(@team), class: "button is-primary is-outlined" do %>
                <span class="icon"><i class="fa fa-chevron-left"></i></span> <%= @team.name %>
                <% end %>
                <% if owner? @team %>
                <%= link_to [@team, @developer], method: :delete, class: "button is-danger", data: { confirm: "You sure?" } do %>
                <span class="icon"><i class="fa fa-trash"></i></span> Remove
                <% end %>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
<br>
<section>
  <div class="container">
    <%= render @developer.github_user %>
  </div>
</section>
<br>
<section>
  <div class="columns is-multiline">
    <% unless @recent_tweets.nil? %>
      <%= render 'datapanels/twitter', object: @recent_tweets %>
    <% end %>

    <% unless @recent_stories.nil? %>
      <%= render 'datapanels/medium', object: @recent_stories %>
    <% end %>
  </div>
</section>
